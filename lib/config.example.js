const config = {};

config.USERNAME = 'your smartjailmail.com username';
config.PASSWORD = 'your smartjailmail.com password';
config.FULLNAME = 'display name (used with TO: and FROM:)';

module.exports = config;